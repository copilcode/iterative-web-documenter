# Anthropic API Key — required for Claude AI calls
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Server port (default: 3001)
PORT=3001

# Directory for JSON project files (default: ./data)
DATA_DIR=./data
